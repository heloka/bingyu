# 并语 0.1.13

这一版加入默认开启的内存节省模式，减少打开多个标签后长期保留 WebView2 渲染器造成的内存占用。

- 隐藏标签会立即暂停网页活动，持续隐藏 60 秒后释放对应的 WebView2。
- 切回已释放的标签时按原网址重新载入，共享账号、Cookie 和站点数据继续保留。
- 隐藏整个并语窗口时，同样会暂停所有网页；重新显示后恢复当前分屏。
- 设置中可关闭“自动释放后台标签”。关闭后，后台页面继续保留；已释放的页面会在下次切回时创建。
- 修正快速切换标签时，WebView2 异步暂停晚于界面切换而导致当前页面仍处于暂停状态的问题。
- 设置页版本号改为读取程序版本，避免升级后显示旧版本。

内存节省会重新载入长期隐藏的页面，所以网页中尚未提交的文字可能丢失。正在分屏中同时显示的网页仍各自运行一个 WebView2 渲染器；三个复杂 AI 网页同时显示时，内存占用仍可能接近同时打开三个浏览器标签。
