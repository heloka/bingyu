# 并语 0.1.9 · Windows x64

并语将 ChatGPT、Claude、Gemini、DeepSeek、豆包、千问、智谱清言及自定义网站放在同一工作台，支持同站多开、2/3/4 分屏拖动、单页专注模式和全局快捷键。

本版统一了「并语」的窗口、托盘、弹窗与发布文件名称；去掉常驻底栏，将顶栏显隐收拢到侧栏底部，并让 Gemini 面板边缘与其网页底色相接。旧版 `%LOCALAPPDATA%\Qiye\` 配置与登录数据会继续使用。

下载选择：

- `Bingyu-win-x64.zip`：约 0.8 MB；需 .NET 10 Desktop Runtime 和 WebView2 Runtime。
- `Bingyu-win-x64-self-contained.zip`：约 72 MB；自带 .NET，仍需 WebView2 Runtime。

两个包均已在 Windows 上完成 16 项核心测试和 19 项桌面检查。首次在另一台电脑使用，请看 [QUICKSTART.md](QUICKSTART.md)。
